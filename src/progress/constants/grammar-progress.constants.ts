import { GRAMMAR_ANSWER_XP } from '../../gamification/constants/grammar-gamification.constants';

/** XP tracked per correct grammar answer (matches gamification award). */
export const GRAMMAR_XP_PER_CORRECT = GRAMMAR_ANSWER_XP;